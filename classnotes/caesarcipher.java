import java.io.*;

class caesarcipher{
    public static void main(String args[])
    {

    }
}



for (int i = 0; i < message.length(); ++i)
{
    ch = message.charAt(i);
    if (ch >= 'a' && ch <+ 'z')
    {
        ch = (char)(ch + key);
    }
}